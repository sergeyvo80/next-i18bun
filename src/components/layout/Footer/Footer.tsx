import styles from './Footer.module.scss';

const Header = (): React.ReactElement => (
  <footer className={styles.Footer}>
    stack: bun, nextjs, i18next, jwt
  </footer>
);

export default Header;
